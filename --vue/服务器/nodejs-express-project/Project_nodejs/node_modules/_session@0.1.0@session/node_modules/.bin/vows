#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_vows@0.8.1@vows/bin/vows" "$@"
  ret=$?
else 
  node  "$basedir/../../../_vows@0.8.1@vows/bin/vows" "$@"
  ret=$?
fi
exit $ret
